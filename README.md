# kafka load balancer config

## Install Nginx Package
```bash
sudo apt install nginx-core
```

## Update /etc/nginx/nginx.conf file based upon this template file: 
   [`nginx.conf`](nginx.conf)

```bash
sudo systemctl restart nginx
```


